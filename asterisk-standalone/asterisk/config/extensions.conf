; Standalone: direct SIP extension-to-extension calling

[general]
static=yes
writeprotect=no

[default]
exten => _X.,1,NoOp(Standalone call from ${CALLERID(num)} to ${EXTEN})
 same => n,Dial(PJSIP/${EXTEN},120)
 same => n,Hangup()
