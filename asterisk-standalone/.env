# Copy to .env and fill in before: docker compose --env-file .env up -d --build

# Public IP or DNS that clients use to reach this Asterisk server.
#EXTERNAL_IP=203.0.113.10
EXTERNAL_IP=192.168.1.111

# Two default extensions for quick softphone-to-softphone testing.
SIP_EXTEN_1=1001
SIP_PASSWORD_1=p1
SIP_EXTEN_2=70141
SIP_PASSWORD_2=Ee.123
